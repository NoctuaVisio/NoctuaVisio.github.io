{
  "_doc": "Hero carousel = ordered list of inspection slugs. Each entry references /inspections/<slug>.json (same format admin generates). Landing displays the model + rotation + offset + markers from each. Add slugs to enable prev/next + dots.",
  "inspections": ["landing-warehouse"]
}
